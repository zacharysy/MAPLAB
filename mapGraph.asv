% Produce graph and highlight nodes

clear
clc


img = imread('sample.png');
imshow(img);
title('Campus Sample');
[x, y] = ginput();

hold on;
for i=1:length(x)
    plot([x(i), x(i+1)], 'r-');
    hold on;
end
